# Docker-specific configuration
# Inherits from application.properties and overrides necessary values

# Database Configuration (Docker)
spring.datasource.url=jdbc:postgresql://postgres:5432/leave_management
spring.datasource.username=postgres
spring.datasource.password=postgres

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
# Bulk Upload Configuration (Docker)
# Directory where bulk upload result CSV files will be stored in Docker
bulk.upload.result-dir=/app/bulk-upload-results
